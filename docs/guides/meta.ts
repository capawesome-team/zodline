import { defineMeta } from 'blume';

export default defineMeta({
  title: 'Guides',
  icon: 'compass',
  pages: ['commands', 'options', 'arguments', 'parsing-vs-execution', 'help-and-version', 'error-handling'],
});
