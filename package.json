{
  "name": "zodline",
  "version": "0.2.0",
  "description": "A CLI parser built with Zod.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "zodline-example": "./example.js"
  },
  "files": [
    "dist"
  ],
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/robingenz/zodline.git"
  },
  "bugs": {
    "url": "https://github.com/robingenz/zodline/issues"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist",
    "fmt": "prettier --write \"src/**/*.{ts,js,json}\"",
    "lint": "prettier --check \"src/**/*.{ts,js,json}\"",
    "test": "vitest run",
    "test:run": "vitest run",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage",
    "release": "commit-and-tag-version",
    "release:dry-run": "npm run release -- --dry-run",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "@vitest/coverage-v8": "^3.2.7",
    "@vitest/ui": "^3.2.7",
    "commit-and-tag-version": "^12.7.3",
    "prettier": "^3.9.6",
    "rimraf": "^5.0.5",
    "typescript": "^5.9.3",
    "vitest": "^3.2.7",
    "zod": "^4.4.3"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "keywords": [
    "cli",
    "parser",
    "zod",
    "typescript",
    "command-line"
  ],
  "author": "Robin Genz <mail@robingenz.dev>",
  "license": "MIT"
}
